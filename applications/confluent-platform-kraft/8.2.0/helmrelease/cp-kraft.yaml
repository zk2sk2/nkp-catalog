# applications/confluent-platform-kraft/8.2.0/helmrelease/cp-kraft.yaml

apiVersion: platform.confluent.io/v1beta1
kind: KRaftController
metadata:
  name: kraftcontroller
  namespace: ${releaseNamespace}
spec:
  # Confluent公式：KRaftControllerは奇数かつ3以上が必須
  replicas: 3
  dataVolumeCapacity: 10Gi
  # ※イメージ指定は、あなたの環境（レジストリ/認証/ポリシー）に合わせて後で追記
---
apiVersion: platform.confluent.io/v1beta1
kind: Kafka
metadata:
  name: kafka
  namespace: ${releaseNamespace}
spec:
  replicas: 1
  dependencies:
    kRaftController:
      clusterRef:
        name: kraftcontroller
  dataVolumeCapacity: 10Gi

  # 1 broker 前提の内部トピック等の replication factor を 1 に寄せる例
  # （Confluentの single-node 例に同種の指定が存在）
  configOverrides:
    server:
      - "offsets.topic.replication.factor=1"
      - "confluent.metadata.topic.replication.factor=1"
      - "confluent.metrics.reporter.topic.replicas=1"
      - "confluent.license.topic.replication.factor=1"
      - "confluent.balancer.topic.replication.factor=1"
      - "confluent.tier.metadata.replication.factor=1"
      - "confluent.security.event.logger.exporter.kafka.topic.replicas=1"
      - "event.logger.exporter.kafka.topic.replicas=1"
``
